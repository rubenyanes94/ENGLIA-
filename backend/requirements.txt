# Paso 1: lo mínimo para levantar la API y confirmar que habla con
# PostgreSQL y Redis. El stack de IA (langgraph, vllm, pgvector client,
# etc.) se añade en el paso donde construyamos el primer agente.

fastapi==0.115.5
uvicorn[standard]==0.32.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
redis==5.2.0
pydantic-settings==2.6.1
alembic==1.14.0
pgvector==0.3.6

# --- Autenticación ---
bcrypt==4.0.1
pyjwt==2.10.1
python-multipart==0.0.17
email-validator==2.2.0

# --- Orquestación de agentes IA ---
langgraph==0.2.60
langchain-core==0.3.28
langchain-openai==0.2.14

# --- Tareas asíncronas (resumen + embedding al cerrar una sesión) ---
celery[redis]==5.4.0
